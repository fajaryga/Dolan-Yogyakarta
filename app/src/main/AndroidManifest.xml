<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fajaryga.doyog">

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@drawable/logoo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/nav_header_subtitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".WisataActivity"
            android:label="@string/wisata"
            android:parentActivityName=".PilihanActivity" />
        <activity
            android:name=".PilihanActivity"
            android:configChanges="orientation|screenLayout|screenSize"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SlemanActivity"
            android:configChanges="orientation|screenLayout|screenSize"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".WslmActivity"
            android:label="@string/wisata"
            android:parentActivityName=".PilihanActivity"/>
        <activity android:name=".info"></activity>
    </application>

</manifest>